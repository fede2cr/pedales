# Running locally

```
$ virtualenv venv # Creates a new Virtual Environment
$ pip install requirements.local
$ export PYTHONPATH=$PWD
$ python cli.py --help
# After running with --video_id, a local directory with the video_id as name will be created and will contain the separated files
```
